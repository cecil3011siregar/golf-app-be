apiVersion: apps/v1
kind: Deployment
metadata:
  name: golf-app-be
  namespace: golf-app-staging
spec:
  selector:
    matchLabels:
      app: golf-app-be
  replicas: 1
  template:
    metadata:
      labels:
        app: golf-app-be
    spec:
      containers:
        - name: golf-app-be
          image: git.empatnusabangsa.com/empatnusabangsa/golf-app-be:<VERSION>
          ports:
            - containerPort: 5000
          envFrom:
            - secretRef:
                name: golf-app-be-env
          # volumeMounts:
          #   - name: storage
          #     mountPath: /home/node/uploads
      # volumes:
      #   - name: storage
      #     persistentVolumeClaim:
      #       claimName: gna-be-pvc
      imagePullSecrets:
        - name: regcred
